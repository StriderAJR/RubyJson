#RubyJson

RubyJson is a simple JSON serializer for Ruby.

I've got the idea of making my own JSON serializer because I needed to store objects in IronRuby. But I found out that
'json' gem was inappropriate to use via .Net for some reasons. So RubyJson supports .Net Json serialization standards.
